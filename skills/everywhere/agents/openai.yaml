interface:
  display_name: "Everywhere"
  short_description: "Use Feishu remote control for local agent sessions"
  default_prompt: "Use Everywhere for the current local agent session. Check the current Feishu binding with `everywhere feishu current --json` when needed. Use `everywhere feishu upload --image/--file` for artifact upload."
