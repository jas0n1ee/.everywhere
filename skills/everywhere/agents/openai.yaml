interface:
  display_name: "Everywhere"
  short_description: "Use Feishu remote control for local agent sessions"
  default_prompt: "Use Everywhere for the current local agent session. Check the current Feishu binding with `everywhere feishu current --json` before artifact upload. Use `lark-cli im +messages-reply --image/--file` from the artifact directory with a relative file name."
